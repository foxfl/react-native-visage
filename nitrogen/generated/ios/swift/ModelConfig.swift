///
/// ModelConfig.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules

/**
 * Represents an instance of `ModelConfig`, backed by a C++ struct.
 */
public typealias ModelConfig = margelo.nitro.visage.ModelConfig

public extension ModelConfig {
  private typealias bridge = margelo.nitro.visage.bridge.swift

  /**
   * Create a new instance of `ModelConfig`.
   */
  init(modelUri: String, embeddingSize: Double, outputLayerName: String?, inputSize: Double?, normalization: ModelNormalization?) {
    self.init(std.string(modelUri), embeddingSize, { () -> bridge.std__optional_std__string_ in
      if let __unwrappedValue = outputLayerName {
        return bridge.create_std__optional_std__string_(std.string(__unwrappedValue))
      } else {
        return .init()
      }
    }(), { () -> bridge.std__optional_double_ in
      if let __unwrappedValue = inputSize {
        return bridge.create_std__optional_double_(__unwrappedValue)
      } else {
        return .init()
      }
    }(), { () -> bridge.std__optional_ModelNormalization_ in
      if let __unwrappedValue = normalization {
        return bridge.create_std__optional_ModelNormalization_(__unwrappedValue)
      } else {
        return .init()
      }
    }())
  }

  @inline(__always)
  var modelUri: String {
    return String(self.__modelUri)
  }
  
  @inline(__always)
  var embeddingSize: Double {
    return self.__embeddingSize
  }
  
  @inline(__always)
  var outputLayerName: String? {
    return { () -> String? in
      if bridge.has_value_std__optional_std__string_(self.__outputLayerName) {
        let __unwrapped = bridge.get_std__optional_std__string_(self.__outputLayerName)
        return String(__unwrapped)
      } else {
        return nil
      }
    }()
  }
  
  @inline(__always)
  var inputSize: Double? {
    return { () -> Double? in
      if bridge.has_value_std__optional_double_(self.__inputSize) {
        let __unwrapped = bridge.get_std__optional_double_(self.__inputSize)
        return __unwrapped
      } else {
        return nil
      }
    }()
  }
  
  @inline(__always)
  var normalization: ModelNormalization? {
    return self.__normalization.value
  }
}

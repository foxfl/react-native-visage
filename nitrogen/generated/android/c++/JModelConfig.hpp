///
/// JModelConfig.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "ModelConfig.hpp"

#include "JModelNormalization.hpp"
#include "ModelNormalization.hpp"
#include <optional>
#include <string>

namespace margelo::nitro::visage {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "ModelConfig" and the the Kotlin data class "ModelConfig".
   */
  struct JModelConfig final: public jni::JavaClass<JModelConfig> {
  public:
    static auto constexpr kJavaDescriptor = "Lcom/margelo/nitro/visage/ModelConfig;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct ModelConfig by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    ModelConfig toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldModelUri = clazz->getField<jni::JString>("modelUri");
      jni::local_ref<jni::JString> modelUri = this->getFieldValue(fieldModelUri);
      static const auto fieldEmbeddingSize = clazz->getField<double>("embeddingSize");
      double embeddingSize = this->getFieldValue(fieldEmbeddingSize);
      static const auto fieldOutputLayerName = clazz->getField<jni::JString>("outputLayerName");
      jni::local_ref<jni::JString> outputLayerName = this->getFieldValue(fieldOutputLayerName);
      static const auto fieldInputSize = clazz->getField<jni::JDouble>("inputSize");
      jni::local_ref<jni::JDouble> inputSize = this->getFieldValue(fieldInputSize);
      static const auto fieldNormalization = clazz->getField<JModelNormalization>("normalization");
      jni::local_ref<JModelNormalization> normalization = this->getFieldValue(fieldNormalization);
      return ModelConfig(
        modelUri->toStdString(),
        embeddingSize,
        outputLayerName != nullptr ? std::make_optional(outputLayerName->toStdString()) : std::nullopt,
        inputSize != nullptr ? std::make_optional(inputSize->value()) : std::nullopt,
        normalization != nullptr ? std::make_optional(normalization->toCpp()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JModelConfig::javaobject> fromCpp(const ModelConfig& value) {
      return newInstance(
        jni::make_jstring(value.modelUri),
        value.embeddingSize,
        value.outputLayerName.has_value() ? jni::make_jstring(value.outputLayerName.value()) : nullptr,
        value.inputSize.has_value() ? jni::JDouble::valueOf(value.inputSize.value()) : nullptr,
        value.normalization.has_value() ? JModelNormalization::fromCpp(value.normalization.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::visage

///
/// HybridVisageSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `FaceDetection` to properly resolve imports.
namespace margelo::nitro::visage { struct FaceDetection; }
// Forward declaration of `ScanOptions` to properly resolve imports.
namespace margelo::nitro::visage { struct ScanOptions; }
// Forward declaration of `Album` to properly resolve imports.
namespace margelo::nitro::visage { struct Album; }
// Forward declaration of `ModelConfig` to properly resolve imports.
namespace margelo::nitro::visage { struct ModelConfig; }

#include "FaceDetection.hpp"
#include <vector>
#include <NitroModules/Promise.hpp>
#include <string>
#include <NitroModules/ArrayBuffer.hpp>
#include "ScanOptions.hpp"
#include "Album.hpp"
#include "ModelConfig.hpp"

namespace margelo::nitro::visage {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Visage`
   * Inherit this class to create instances of `HybridVisageSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridVisage: public HybridVisageSpec {
   * public:
   *   HybridVisage(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridVisageSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridVisageSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridVisageSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::vector<FaceDetection>>> extractEmbeddings(const std::string& imageUri) = 0;
      virtual std::shared_ptr<Promise<double>> compareFaces(const std::shared_ptr<ArrayBuffer>& a, const std::shared_ptr<ArrayBuffer>& b) = 0;
      virtual std::shared_ptr<Promise<void>> scanLibrary(const ScanOptions& options) = 0;
      virtual std::shared_ptr<Promise<void>> cancelScan() = 0;
      virtual std::shared_ptr<Promise<std::vector<Album>>> getAlbums() = 0;
      virtual std::shared_ptr<Promise<void>> setModel(const ModelConfig& config) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Visage";
  };

} // namespace margelo::nitro::visage
